# ai tools

Claude.ai liefert valides sql ohne anpassungen. 
allerdings können nicht viele daten auf einmal generiert werden.

```
Gegeben ist folgendes sqlite db modell

<insert create.sql here>

Bitte erstelle mit eine sql insert datei die folgendes enthält

* 3 agenten
* 30 immobilien in spanien an der costa blanka
* die texte alle auf englisch
* images soll mindestens zwei http links zu pexels haben (komma getrennt)
* die links zu den pexel bildern sollten gültig sein
* die images sollte ein JSON array sein
* es sollten mindestens 2 bilder enthalten sein
```